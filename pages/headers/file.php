<?php

/**
 * ____________________________________________________________________________________________
 * 
 *                                  *** 文件管理头文件 ***
 * 
 *                          该文件包含文件管理相关的函数和常量定义
 *                          包括文件上传、查询、删除、信息获取等操作
 *                          如需使用文件管理相关函数,请先引入该文件
 *                           
 *                          除了简便api,其他相关函数都以File_开头
 * 
 * -----------------------------------------------------------------------------------
 * 
 *                                     *** 注意事项 ***
 * 
 *                  该库里所有的获取信息类、信息修改类、文件创建类的函数，
 *                  都会返回File类的实例对象。相关信息已经过封装，如需获取具体信息，
 *                  请调用该库的相关方法获取，不建议直接访问File类的属性。
 * 
 * +---------------------------------------函数定义------------------------------------
 * |                    +=============================================+
 * |                    |         这里是对文件管理相关函数的简单定义        |
 * |                    |      如需详细了解每个函数的参数和返回值,          |
 * |                    |            请参考文档，或相关IDE的自动提示       |
 * |                    +=============================================+
 * |
 * |
 * |--------- 文件查询 ----->
 * |    File_get_file($key)                ---------------------- 根据标识符获取单个文件
 * |    File_get_all_file($key)            ---------------------- 根据标识符获取所有匹配文件
 * |    File_id_to_path($key)              ---------------------- 根据文件ID获取文件路径
 * |
 * |
 * |--------- 文件操作 ----->
 * |    File_create($path)                 ---------------------- 创建文件映射
 * |    File_save($FILE)                   ---------------------- 处理文件上传并创建映射
 * |    File_delete($file)                 ---------------------- 删除文件
 * |    File_update($file)                 ---------------------- 更新文件信息
 * |
 * |
 * |------ 获取文件信息 -->
 * |    File_get_id($file)                 ---------------------- 获取文件ID
 * |    File_get_path($file)               ---------------------- 获取文件路径
 * |    File_get_type($file)               ---------------------- 获取文件类型
 * |    File_get_name($file)               ---------------------- 获取文件名
 * |    File_get_size($file)               ---------------------- 获取文件大小
 * |    File_get_time($file)               ---------------------- 获取文件上传时间
 * |    File_get_user_id($file)            ---------------------- 获取文件所属用户ID
 * |    File_get_status($file)             ---------------------- 获取文件状态
 * |
 * |
 * |------ 设置文件信息 -->
 * |    File_set_path($file, $path)        ---------------------- 设置文件路径
 * |    File_set_type($file, $type)        ---------------------- 设置文件类型
 * |    File_set_name($file, $name)        ---------------------- 设置文件名
 * |    File_set_time($file, $time)        ---------------------- 设置文件上传时间
 * |    File_set_user_id($file, $user)     ---------------------- 设置文件所属用户ID
 * |
 * L___________________________________________________________________________________________________    
 */

// 引入预处理库
include_once __DIR__ . '/../../include/_PRE.php';

include_once __DIR__ . '/header.php';
include_once __DIR__ . '/../../include/conn.php';
include_once __DIR__ . '/../../libs/function/file.php';